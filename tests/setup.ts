// Test setup file
// Increase timeout for integration tests
jest.setTimeout(10000);

// Global test setup
beforeAll(() => {
  // Any global setup needed for all tests
});

afterAll(() => {
  // Any global cleanup
});
